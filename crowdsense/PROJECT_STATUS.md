# PROJECT_STATUS.md — CrowdSense Codebase Health Report
> Generated by: Session 9 full audit of crowdsense-clean.zip  
> Date: June 2026  
> Source of truth: ZIP codebase (code > documentation where they conflict)

---

## Overall Health: 🟡 MOSTLY WORKING — Phase 4 UI incomplete

The backend is architecturally complete and working. The frontend has all the right files but several pages have structural or styling gaps. The ML service is functional. The edge agent is complete with simulation mode. The system can be started and data will flow end-to-end.

---

## ✅ WORKING — Confirmed Functional

### Backend (Spring Boot)
| Component | Status | Notes |
|-----------|--------|-------|
| Application startup | ✅ Working | Connects to Supabase transaction pooler |
| JWT auth (`/auth/login`, `/auth/me`) | ✅ Working | Returns `{token, role, email}` |
| `DataInitializer` | ✅ Fixed | Seeds admin + organizer + loc_001 + demo event |
| `POST /readings` | ✅ Working | Accepts sensor readings, broadcasts via WS |
| `GET /readings/health` | ✅ Working | Returns `{status: UP}` |
| `GET /readings/latest` | ✅ Fixed | DISTINCT ON native query — no more 500 error |
| `GET /readings/location/{id}` | ✅ Working | History endpoint |
| `POST /sync/bulk` | ✅ Working | SQLite buffer upload |
| `GET /events` | ✅ Working | Paginated public listing |
| `GET /events/search` | ✅ Working | FTS + geo search (PostGIS) |
| `GET /events/{id}` | ✅ Working | Detail with zones + crowd summary |
| `GET /events/mine` | ✅ Working | Organiser's events (JWT gated) |
| `POST /events` | ✅ Working | Create event |
| `PUT /events/{id}/status` | ✅ Working | DRAFT → LIVE → ENDED |
| `GET /locations` | ✅ Working | Zone list (with optional eventId filter) |
| `POST /locations` | ✅ Working | Create zone (JWT gated) |
| `GET /alerts` | ✅ Working | Filter: active/resolved/all |
| `PUT /alerts/{id}/resolve` | ✅ Working | Mark resolved |
| `GET /route` | ✅ Working | Proxies to ML; fallback route if ML down |
| `POST /route/check` | ✅ Working | Reroute decision |
| `GET /recommend` | ✅ Working | Stall recommendations |
| WebSocket STOMP | ✅ Working | Native WS, topics: /topic/crowd, /topic/alerts, /topic/sensor-status |
| AlertService | ✅ Working | Fires alert on HIGH/CRITICAL |
| WebSocket broadcast | ✅ Working | Broadcasts on every reading saved |
| Security config | ✅ Working | Correct public/protected route mapping |

### Frontend (React)
| Component | Status | Notes |
|-----------|--------|-------|
| `App.jsx` routing | ✅ Working | Dual routes /dashboard/* and /app/* |
| `Login.jsx` | ✅ Working | Dual-tab (organiser/attendee), JWT stored |
| `DashboardLayout.jsx` | ✅ Working | Sidebar nav with correct links |
| `EventManager.jsx` | ✅ Working | List events, create event, status transitions |
| `AlertCenter.jsx` | ✅ Working | Filter alerts, resolve, WS live push |
| `ZoneEditor.jsx` | ✅ Working | Leaflet.draw polygon tool, saves to backend |
| `Analytics.jsx` | ✅ Working | Bar chart of peak counts per zone |
| `AppLayout.jsx` | ✅ Working | Attendee layout with outlet |
| `EventSearch.jsx` | ✅ Working | Search + geolocation + LIVE events on load |
| `EventDetail.jsx` | ✅ Working | Zone map + crowd dots + 5s refresh + WS |
| `StallBrowser.jsx` | ✅ Working | Stall cards with ML recommendations |
| `RoutingView.jsx` | ✅ Working | Stall picker + Leaflet map + waypoints + reroute |
| `websocketClient.js` | ✅ Working | Native WS, no SockJS, event handlers |
| `useWebSocket.js` | ✅ Working | crowdMap + latestAlert + sensorStatus |
| `crowdApi.js` | ✅ Working | All API calls, JWT interceptor |
| `crowdDots.js` | ✅ Working | Random polygon distribution + real pos fallback |
| `useGeolocation.js` | ✅ Working | Permission check + request flow |
| `useProximityAlert.js` | ✅ Working | 50m CRITICAL zone detection |

### Edge Agent (Python)
| Component | Status | Notes |
|-----------|--------|-------|
| `sensor_agent.py` | ✅ Working | SIMULATION / DEMO_VIDEO / LIVE modes |
| `local_db.py` | ✅ Fixed | Duplicate function removed |
| `sync_service.py` | ✅ Working | Offline detection + bulk upload |
| `fusion_engine.py` | ✅ Working | Kalman filter camera+WiFi fusion |
| `crowd_detector.py` | ✅ Working | YOLOv8 + simulate_crowd_reading |
| `wifi_probe.py` | ✅ Working | Simulated correlated WiFi |
| `local_alert.py` | ✅ Working | Internet-independent alerts |
| `mqtt_client.py` | ✅ Working | Optional MQTT publish |
| `config.py` | ✅ Working | BACKEND_URL, LOCATION_ID, modes |

### ML Service (Python FastAPI)
| Component | Status | Notes |
|-----------|--------|-------|
| `ml/main.py` | ✅ Working | FastAPI app, all endpoints |
| `ml/router.py` | ✅ Working | Dijkstra algorithm |
| `ml/recommender.py` | ✅ Working | Stall scoring |
| `GET /health` | ✅ Working | Liveness |
| `POST /route` | ✅ Working | Dijkstra route |
| `POST /route/check` | ✅ Working | Reroute decision |
| `POST /recommend` | ✅ Working | Ranked stall list |

---

## 🔴 BROKEN — Known Bugs/Issues

| Item | Severity | Description | Fix |
|------|----------|-------------|-----|
| Duplicate main class | HIGH | `BackendApplication.java` in `com.crowdsense.backend` package conflicts with `CrowdSenseApplication.java` in `com.crowdsense`. Maven builds the wrong one. | Delete `backend/src/main/java/com/crowdsense/backend/BackendApplication.java` and `BackendApplicationTests.java` |
| `dashboard/Dashboard.jsx` is empty | HIGH | The main overview page `pages/dashboard/Dashboard.jsx` exists but is empty (0 bytes). The DashboardLayout `index` route points here — going to `/dashboard` shows blank. | **Build this file** — see TASKS.md Task 1 |
| `EventSearch.jsx` demo hint missing | LOW | Demo hint says "organizer@demo.com" in Login.jsx but the actual hint text in EventSearch.jsx is correct. Minor inconsistency. | Cosmetic fix |
| `StallBrowser.jsx` needs `StallManager.jsx` | MEDIUM | The organiser has no stall management UI in the dashboard (only the attendee has StallBrowser). `StallManager.jsx` is referenced in DashboardLayout but doesn't exist as a file. | Build `StallManager.jsx` or add stall CRUD to EventManager |
| Backend `BackendApplicationTests.java` | MEDIUM | Wrong package, will fail Maven test phase | Delete file |
| Multiple `yolov8n.pt` copies | LOW | Model file is in both `edge/` and `edge/models/`. Wastes disk space. | Delete `edge/yolov8n.pt`, keep `edge/models/yolov8n.pt` only |
| `online/` and `sync/` directories | LOW | Orphaned test directories in project root — not referenced by anything | Delete them |
| 8+ test_pooler_*.py files | LOW | Debugging artifacts from connection troubleshooting. Not needed anymore. | Delete all `backend/test_*.py` files |
| `frontend/.env` has placeholder | LOW | `VITE_BACKEND_URL=http://localhost:8080` — correct for local dev but not documented in `.env.example` clearly | Minor docs update |

---

## 🟡 INCOMPLETE — Built but Missing Key Parts

| Item | What Exists | What's Missing |
|------|-------------|----------------|
| Dashboard overview (`/dashboard`) | File exists but empty | Stats cards, live chart, zone status table, sensor status indicator |
| Zone connections UI | `ZoneConnectionController` + `ZoneConnectionService` exist in backend | No frontend UI to draw connections. Organiser cannot define walking paths between zones yet. Routing uses synthetic connections. |
| Stall management in dashboard | `StallController`, `StallService`, `StallRepository` all exist | `StallManager.jsx` missing from frontend |
| `CrowdDotLayer.jsx` component | Dots generated in `EventDetail.jsx` inline | Standalone canvas component promised in v7 but not extracted. Logic is inline in EventDetail — works but not reusable. |
| Camera calibration tool | Mentioned in master doc | Not started. Required for real position mapping in production. |
| CSRNet dense crowd model | Mentioned in master doc | Not integrated. YOLOv8 only for now (fine for demo). |
| Demo video replay | `generate_demo_data.py` + `sensor_agent.py` DEMO_VIDEO mode exist | No demo video file provided. Mode works but requires `edge/demo_data/readings_cache.csv` |
| FCM push notifications | `NotificationService.java` exists, firebase config in properties | firebase.enabled=false. No firebase-service-account.json. No FCM implementation in service. |
| Redis Streams | Mentioned in v7, `RedisConfig.java` missing | Not implemented. Direct synchronous processing is used. Fine for now. |
| Rate limiting (bucket4j) | Mentioned in v7 | Not implemented |
| Deployment configs | `application-prod.properties` exists | No Dockerfile, no Render/Vercel config files, no CI/CD |
| Stall CRUD for organisers | Backend endpoints exist | Dashboard UI missing |
| `useRoute.js` hook | Mentioned in v7 | Not created — routing logic is directly in `RoutingView.jsx` (acceptable) |

---

## 📊 Code Quality Assessment

| Area | Score | Notes |
|------|-------|-------|
| Backend architecture | 9/10 | Clean separation of concerns, correct patterns |
| Backend error handling | 7/10 | RouteService has good fallbacks, other services minimal |
| Frontend code quality | 8/10 | Clean hooks, good state management |
| Frontend accessibility | 4/10 | No ARIA labels, keyboard nav not tested |
| Frontend performance | 7/10 | 5s polling is fine, canvas dots could jitter |
| Test coverage | 1/10 | Almost zero automated tests |
| Security | 7/10 | JWT working, CORS is `*` (fine for dev, needs locking for prod) |
| Offline resilience | 9/10 | SQLite buffer + Kalman fallback + MQTT mesh all implemented |
| Demo readiness | 7/10 | Needs dashboard overview page + stall manager |

---

## 🚀 Recommended Next Build Priority

1. **Fix: Delete duplicate main class** (5 min) — backend won't build reliably without this
2. **Build: `dashboard/Dashboard.jsx`** (2h) — the overview page is blank
3. **Build: `dashboard/StallManager.jsx`** (2h) — organiser needs to add stalls
4. **Polish: Login page UI** (1h) — functional but plain
5. **Polish: Overall design system** (3h) — all pages use inline styles, inconsistent
6. **Build: Zone connection editor** (3h) — lets organiser define walking paths

See `TASKS.md` for the full ordered list.
